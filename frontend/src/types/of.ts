export type OfOrder = {
  ofNumber: string;
  partCode: string;
  partNumber: string;
  date: string;
  quantity: number;
  originalQuantity: number;
  serialNumber: number | null;
  observation: string;
};
